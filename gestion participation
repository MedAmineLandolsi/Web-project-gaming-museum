<?php
include_once '../config/database.php';
include_once '../models/Participation.php';
include_once '../controllers/ParticipationController.php';

$database = new Database();
$db = $database->getConnection();

$participationController = new ParticipationController($db);
$participations = $participationController->index();
?>

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestion des Participations - Administration</title>
    <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=VT323&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-green: #00FF41;
            --secondary-purple: #BD00FF;
            --accent-pink: #FF006E;
            --warning-orange: #FF9500;
            --dark-bg: #0a0a0a;
            --darker-bg: #050505;
            --card-bg: #1a1a1a;
            --sidebar-bg: #0d0d0d;
            --text-white: #ffffff;
            --text-gray: #888888;
            --text-light-gray: #aaaaaa;
            --border-color: #333333;
            --success-green: #00FF41;
            --danger-red: #FF0055;
        }

        body {
            font-family: 'Press Start 2P', cursive;
            background-color: var(--dark-bg);
            background-image: 
                repeating-linear-gradient(
                    0deg,
                    transparent,
                    transparent 2px,
                    rgba(0, 255, 65, 0.02) 2px,
                    rgba(0, 255, 65, 0.02) 4px
                );
            color: var(--text-white);
            line-height: 1.6;
            display: flex;
            min-height: 100vh;
        }

        /* Main Content */
        .main-content {
            flex: 1;
            margin-left: 0;
            padding: 2rem;
            margin-top: 80px;
        }

        /* Top Bar */
        .top-bar {
            background: linear-gradient(135deg, var(--sidebar-bg), var(--darker-bg));
            border: 2px solid var(--primary-green);
            padding: 1.5rem;
            margin-bottom: 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 5px 30px rgba(0, 255, 65, 0.2);
        }

        .top-bar-left {
            display: flex;
            align-items: center;
            gap: 1.5rem;
        }

        .page-title {
            font-size: 1.2rem;
            color: var(--primary-green);
            text-shadow: 0 0 10px var(--primary-green);
        }

        .top-bar-right {
            display: flex;
            align-items: center;
            gap: 1.5rem;
        }

        .btn-view-site {
            padding: 0.8rem 1.5rem;
            background: linear-gradient(135deg, var(--primary-green), #00cc33);
            color: var(--darker-bg);
            border: none;
            font-family: 'Press Start 2P', cursive;
            font-size: 0.6rem;
            cursor: pointer;
            transition: all 0.3s;
            font-weight: bold;
            text-decoration: none;
        }

        .btn-view-site:hover {
            box-shadow: 0 0 30px rgba(0, 255, 65, 0.6);
            transform: translateY(-2px);
        }

        /* Header Section */
        .page-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            padding: 1.5rem;
            background: linear-gradient(135deg, var(--card-bg), var(--darker-bg));
            border: 2px solid var(--secondary-purple);
            box-shadow: 0 5px 30px rgba(189, 0, 255, 0.2);
        }

        .page-header h1 {
            font-size: 1.2rem;
            color: var(--secondary-purple);
            text-shadow: 0 0 10px var(--secondary-purple);
        }

        .stats-badge {
            padding: 0.8rem 1.5rem;
            background: linear-gradient(135deg, var(--secondary-purple), #9900cc);
            color: var(--text-white);
            border: none;
            font-family: 'Press Start 2P', cursive;
            font-size: 0.6rem;
            font-weight: bold;
            border-radius: 3px;
        }

        /* Table Container */
        .table-container {
            background: linear-gradient(135deg, var(--card-bg), var(--darker-bg));
            border: 2px solid var(--border-color);
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0, 255, 65, 0.1);
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        thead {
            background-color: rgba(189, 0, 255, 0.1);
            border-bottom: 2px solid var(--secondary-purple);
        }

        th {
            padding: 1.5rem 1rem;
            text-align: left;
            font-size: 0.6rem;
            color: var(--secondary-purple);
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        td {
            padding: 1.2rem 1rem;
            border-bottom: 1px solid var(--border-color);
            font-size: 0.7rem;
            font-family: 'VT323', monospace;
            font-size: 1rem;
        }

        tbody tr {
            transition: all 0.3s;
        }

        tbody tr:hover {
            background-color: rgba(189, 0, 255, 0.05);
            transform: scale(1.01);
        }

        /* ID Styling */
        .participation-id {
            color: var(--primary-green);
            text-shadow: 0 0 5px var(--primary-green);
            font-weight: bold;
            font-family: 'VT323', monospace;
            font-size: 1.1rem;
        }

        /* Event Name Styling */
        .event-name {
            color: var(--accent-pink);
            font-weight: bold;
        }

        /* Participant Info Styling */
        .participant-name {
            color: var(--text-white);
            font-weight: bold;
        }

        .participant-email {
            color: var(--primary-green);
            font-family: 'VT323', monospace;
        }

        .participant-phone {
            color: var(--warning-orange);
            font-family: 'VT323', monospace;
        }

        /* Date Styling */
        .inscription-date {
            color: var(--text-light-gray);
            font-family: 'VT323', monospace;
        }

        /* Action Buttons */
        .btn-delete {
            padding: 0.5rem 1rem;
            font-size: 0.5rem;
            text-decoration: none;
            border: 2px solid var(--danger-red);
            color: var(--danger-red);
            background: transparent;
            transition: all 0.3s;
            display: inline-block;
            font-family: 'Press Start 2P', cursive;
            cursor: pointer;
        }

        .btn-delete:hover {
            background-color: var(--danger-red);
            color: var(--text-white);
            box-shadow: 0 0 15px rgba(255, 0, 85, 0.5);
            transform: translateY(-2px);
        }

        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 3rem;
            color: var(--text-gray);
            font-size: 0.8rem;
        }

        .empty-state .icon {
            font-size: 3rem;
            margin-bottom: 1rem;
            color: var(--secondary-purple);
        }

        /* Status Indicators */
        .status-badge {
            padding: 0.3rem 0.6rem;
            font-size: 0.5rem;
            border-radius: 3px;
            display: inline-block;
            margin-left: 0.5rem;
        }

        .status-confirmed {
            background-color: rgba(0, 255, 65, 0.2);
            border: 1px solid var(--success-green);
            color: var(--success-green);
        }

        .status-pending {
            background-color: rgba(255, 149, 0, 0.2);
            border: 1px solid var(--warning-orange);
            color: var(--warning-orange);
        }

        /* Responsive Design */
        @media (max-width: 1200px) {
            .table-container {
                overflow-x: auto;
            }
            
            table {
                min-width: 1000px;
            }
        }

        @media (max-width: 1024px) {
            .main-content {
                padding: 1rem;
            }

            .page-header {
                flex-direction: column;
                gap: 1rem;
                text-align: center;
            }

            th, td {
                padding: 1rem 0.8rem;
            }
        }

        @media (max-width: 768px) {
            .page-header h1 {
                font-size: 0.9rem;
            }

            .stats-badge {
                padding: 0.6rem 1rem;
                font-size: 0.5rem;
            }

            th {
                font-size: 0.5rem;
                padding: 1rem 0.5rem;
            }

            td {
                font-size: 0.6rem;
                padding: 0.8rem 0.5rem;
            }

            .btn-delete {
                padding: 0.4rem 0.8rem;
                font-size: 0.4rem;
            }
        }

        @media (max-width: 480px) {
            .main-content {
                padding: 0.5rem;
                margin-top: 60px;
            }

            .btn-view-site {
                display: none;
            }

            .page-header {
                padding: 1rem;
            }

            .empty-state {
                padding: 2rem 1rem;
                font-size: 0.6rem;
            }
        }

        /* Scrollbar Styling */
        ::-webkit-scrollbar {
            width: 10px;
            height: 10px;
        }

        ::-webkit-scrollbar-track {
            background: var(--darker-bg);
            border-left: 1px solid var(--border-color);
        }

        ::-webkit-scrollbar-thumb {
            background: linear-gradient(180deg, var(--secondary-purple), var(--primary-green));
            border-radius: 5px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(180deg, #9900cc, #00cc33);
        }
    </style>
</head>
<body>
    <?php include_once 'views/back/header.php'; ?>

    <div class="main-content">
        <div class="top-bar">
            <div class="top-bar-left">
                <h1 class="page-title">GESTION DES PARTICIPATIONS</h1>
            </div>
            <div class="top-bar-right">
                <a href="../index.php" class="btn-view-site">VOIR LE SITE</a>
            </div>
        </div>

        <div class="page-header">
            <h1>INSCRIPTIONS AUX ÉVÉNEMENTS</h1>
            <div class="stats-badge">
                TOTAL: <?php echo count($participations); ?> INSCRIPTIONS
            </div>
        </div>

        <div class="table-container">
            <?php if (empty($participations)): ?>
                <div class="empty-state">
                    <div class="icon">👥</div>
                    <p>AUCUNE INSCRIPTION TROUVÉE</p>
                    <p style="margin-top: 1rem; font-size: 0.6rem; color: var(--text-gray);">
                        Les inscriptions aux événements apparaîtront ici
                    </p>
                </div>
            <?php else: ?>
                <table>
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>ÉVÉNEMENT</th>
                            <th>PARTICIPANT</th>
                            <th>EMAIL</th>
                            <th>TÉLÉPHONE</th>
                            <th>DATE INSCRIPTION</th>
                            <th>ACTIONS</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach($participations as $participation): ?>
                        <tr>
                            <td><span class="participation-id">#<?php echo $participation['id_participation']; ?></span></td>
                            <td><span class="event-name"><?php echo htmlspecialchars($participation['evenement_nom']); ?></span></td>
                            <td><span class="participant-name"><?php echo htmlspecialchars($participation['nom_participant']); ?></span></td>
                            <td><span class="participant-email"><?php echo htmlspecialchars($participation['email']); ?></span></td>
                            <td><span class="participant-phone"><?php echo htmlspecialchars($participation['telephone']); ?></span></td>
                            <td><span class="inscription-date"><?php echo date('d/m/Y H:i', strtotime($participation['date_inscription'])); ?></span></td>
                            <td>
                                <a href="participations/delete.php?id=<?php echo $participation['id_participation']; ?>" class="btn-delete" onclick="return confirm('Êtes-vous sûr ?')">SUPPRIMER</a>
                            </td>
                        </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            <?php endif; ?>
        </div>
    </div>

    <?php include_once 'views/back/footer.php'; ?>
</body>
</html>