<?php
include_once '../config/database.php';
include_once '../models/Evenement.php';
include_once '../controllers/EvenementController.php';

$database = new Database();
$db = $database->getConnection();

$evenementController = new EvenementController($db);
$evenements = $evenementController->index();
?>

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestion des Événements - Administration</title>
    <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=VT323&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-green: #00FF41;
            --secondary-purple: #BD00FF;
            --accent-pink: #FF006E;
            --warning-orange: #FF9500;
            --dark-bg: #0a0a0a;
            --darker-bg: #050505;
            --card-bg: #1a1a1a;
            --sidebar-bg: #0d0d0d;
            --text-white: #ffffff;
            --text-gray: #888888;
            --text-light-gray: #aaaaaa;
            --border-color: #333333;
            --success-green: #00FF41;
            --danger-red: #FF0055;
        }

        body {
            font-family: 'Press Start 2P', cursive;
            background-color: var(--dark-bg);
            background-image: 
                repeating-linear-gradient(
                    0deg,
                    transparent,
                    transparent 2px,
                    rgba(0, 255, 65, 0.02) 2px,
                    rgba(0, 255, 65, 0.02) 4px
                );
            color: var(--text-white);
            line-height: 1.6;
            display: flex;
            min-height: 100vh;
        }

        /* Main Content */
        .main-content {
            flex: 1;
            margin-left: 0;
            padding: 2rem;
            margin-top: 80px;
        }

        /* Top Bar */
        .top-bar {
            background: linear-gradient(135deg, var(--sidebar-bg), var(--darker-bg));
            border: 2px solid var(--primary-green);
            padding: 1.5rem;
            margin-bottom: 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 5px 30px rgba(0, 255, 65, 0.2);
        }

        .top-bar-left {
            display: flex;
            align-items: center;
            gap: 1.5rem;
        }

        .page-title {
            font-size: 1.2rem;
            color: var(--primary-green);
            text-shadow: 0 0 10px var(--primary-green);
        }

        .top-bar-right {
            display: flex;
            align-items: center;
            gap: 1.5rem;
        }

        .btn-view-site {
            padding: 0.8rem 1.5rem;
            background: linear-gradient(135deg, var(--primary-green), #00cc33);
            color: var(--darker-bg);
            border: none;
            font-family: 'Press Start 2P', cursive;
            font-size: 0.6rem;
            cursor: pointer;
            transition: all 0.3s;
            font-weight: bold;
            text-decoration: none;
        }

        .btn-view-site:hover {
            box-shadow: 0 0 30px rgba(0, 255, 65, 0.6);
            transform: translateY(-2px);
        }

        /* Header Section */
        .page-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            padding: 1.5rem;
            background: linear-gradient(135deg, var(--card-bg), var(--darker-bg));
            border: 2px solid var(--primary-green);
            box-shadow: 0 5px 30px rgba(0, 255, 65, 0.2);
        }

        .page-header h1 {
            font-size: 1.2rem;
            color: var(--primary-green);
            text-shadow: 0 0 10px var(--primary-green);
        }

        .btn-primary {
            padding: 1rem 2rem;
            background: linear-gradient(135deg, var(--primary-green), #00cc33);
            color: var(--darker-bg);
            border: none;
            font-family: 'Press Start 2P', cursive;
            font-size: 0.6rem;
            cursor: pointer;
            transition: all 0.3s;
            font-weight: bold;
            text-decoration: none;
            display: inline-block;
        }

        .btn-primary:hover {
            box-shadow: 0 0 30px rgba(0, 255, 65, 0.6);
            transform: translateY(-2px);
        }

        /* Table Container */
        .table-container {
            background: linear-gradient(135deg, var(--card-bg), var(--darker-bg));
            border: 2px solid var(--border-color);
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0, 255, 65, 0.1);
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        thead {
            background-color: rgba(0, 255, 65, 0.1);
            border-bottom: 2px solid var(--primary-green);
        }

        th {
            padding: 1.5rem 1rem;
            text-align: left;
            font-size: 0.6rem;
            color: var(--primary-green);
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        td {
            padding: 1.2rem 1rem;
            border-bottom: 1px solid var(--border-color);
            font-size: 0.7rem;
            font-family: 'VT323', monospace;
            font-size: 1rem;
        }

        tbody tr {
            transition: all 0.3s;
        }

        tbody tr:hover {
            background-color: rgba(0, 255, 65, 0.05);
            transform: scale(1.01);
        }

        /* Status Badges */
        .places-count {
            font-family: 'Press Start 2P', cursive;
            font-size: 0.6rem;
            padding: 0.3rem 0.6rem;
            border-radius: 3px;
            display: inline-block;
        }

        .places-available {
            background-color: rgba(0, 255, 65, 0.2);
            border: 1px solid var(--success-green);
            color: var(--success-green);
        }

        .places-full {
            background-color: rgba(255, 0, 85, 0.2);
            border: 1px solid var(--danger-red);
            color: var(--danger-red);
        }

        .places-warning {
            background-color: rgba(255, 149, 0, 0.2);
            border: 1px solid var(--warning-orange);
            color: var(--warning-orange);
        }

        /* Price Styling */
        .price {
            color: var(--secondary-purple);
            text-shadow: 0 0 5px var(--secondary-purple);
            font-weight: bold;
        }

        /* Action Buttons */
        .btn-edit, .btn-delete {
            padding: 0.5rem 1rem;
            font-size: 0.5rem;
            text-decoration: none;
            border: 2px solid;
            transition: all 0.3s;
            display: inline-block;
            margin-right: 0.5rem;
            font-family: 'Press Start 2P', cursive;
        }

        .btn-edit {
            background: transparent;
            border-color: var(--primary-green);
            color: var(--primary-green);
        }

        .btn-edit:hover {
            background-color: var(--primary-green);
            color: var(--darker-bg);
            box-shadow: 0 0 15px rgba(0, 255, 65, 0.5);
        }

        .btn-delete {
            background: transparent;
            border-color: var(--danger-red);
            color: var(--danger-red);
        }

        .btn-delete:hover {
            background-color: var(--danger-red);
            color: var(--text-white);
            box-shadow: 0 0 15px rgba(255, 0, 85, 0.5);
        }

        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 3rem;
            color: var(--text-gray);
            font-size: 0.8rem;
        }

        .empty-state .icon {
            font-size: 3rem;
            margin-bottom: 1rem;
            color: var(--primary-green);
        }

        /* Responsive Design */
        @media (max-width: 1200px) {
            .table-container {
                overflow-x: auto;
            }
            
            table {
                min-width: 1000px;
            }
        }

        @media (max-width: 1024px) {
            .main-content {
                padding: 1rem;
            }

            .page-header {
                flex-direction: column;
                gap: 1rem;
                text-align: center;
            }

            th, td {
                padding: 1rem 0.8rem;
            }
        }

        @media (max-width: 768px) {
            .page-header h1 {
                font-size: 0.9rem;
            }

            .btn-primary {
                padding: 0.8rem 1.5rem;
                font-size: 0.5rem;
            }

            th {
                font-size: 0.5rem;
                padding: 1rem 0.5rem;
            }

            td {
                font-size: 0.6rem;
                padding: 0.8rem 0.5rem;
            }

            .btn-edit, .btn-delete {
                padding: 0.4rem 0.8rem;
                font-size: 0.4rem;
                margin-right: 0.3rem;
            }
        }

        @media (max-width: 480px) {
            .main-content {
                padding: 0.5rem;
                margin-top: 60px;
            }

            .btn-view-site {
                display: none;
            }

            .page-header {
                padding: 1rem;
            }

            .empty-state {
                padding: 2rem 1rem;
                font-size: 0.6rem;
            }
        }

        /* Scrollbar Styling */
        ::-webkit-scrollbar {
            width: 10px;
            height: 10px;
        }

        ::-webkit-scrollbar-track {
            background: var(--darker-bg);
            border-left: 1px solid var(--border-color);
        }

        ::-webkit-scrollbar-thumb {
            background: linear-gradient(180deg, var(--primary-green), var(--secondary-purple));
            border-radius: 5px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(180deg, #00cc33, #9900cc);
        }
    </style>
</head>
<body>
    <?php include_once 'views/back/header.php'; ?>

    <div class="main-content">
        <div class="top-bar">
            <div class="top-bar-left">
                <h1 class="page-title">GESTION DES ÉVÉNEMENTS</h1>
            </div>
            <div class="top-bar-right">
                <a href="../index.php" class="btn-view-site">VOIR LE SITE</a>
            </div>
        </div>

        <div class="page-header">
            <h1>LISTE DES ÉVÉNEMENTS</h1>
            <a href="evenements/create.php" class="btn-primary">NOUVEL ÉVÉNEMENT</a>
        </div>

        <div class="table-container">
            <?php if (empty($evenements)): ?>
                <div class="empty-state">
                    <div class="icon">🎮</div>
                    <p>AUCUN ÉVÉNEMENT TROUVÉ</p>
                    <p style="margin-top: 1rem; font-size: 0.6rem; color: var(--text-gray);">
                        Créez votre premier événement pour commencer !
                    </p>
                </div>
            <?php else: ?>
                <table>
                    <thead>
                        <tr>
                            <th>NOM</th>
                            <th>JEU</th>
                            <th>DATE DÉBUT</th>
                            <th>DATE FIN</th>
                            <th>LIEU</th>
                            <th>PLACES</th>
                            <th>PRIX</th>
                            <th>ACTIONS</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php foreach($evenements as $evenement): 
                            $participationsCount = $evenementController->show($evenement['id_evenement'])->countParticipations();
                            $placesClass = '';
                            if ($participationsCount >= $evenement['places_max']) {
                                $placesClass = 'places-full';
                            } elseif ($participationsCount >= $evenement['places_max'] * 0.8) {
                                $placesClass = 'places-warning';
                            } else {
                                $placesClass = 'places-available';
                            }
                        ?>
                        <tr>
                            <td><strong><?php echo htmlspecialchars($evenement['nom']); ?></strong></td>
                            <td><?php echo htmlspecialchars($evenement['jeu']); ?></td>
                            <td><?php echo date('d/m/Y H:i', strtotime($evenement['date_debut'])); ?></td>
                            <td><?php echo date('d/m/Y H:i', strtotime($evenement['date_fin'])); ?></td>
                            <td><?php echo htmlspecialchars($evenement['lieu']); ?></td>
                            <td>
                                <span class="places-count <?php echo $placesClass; ?>">
                                    <?php echo $participationsCount; ?> / <?php echo $evenement['places_max']; ?>
                                </span>
                            </td>
                            <td><span class="price"><?php echo $evenement['prix']; ?>€</span></td>
                            <td>
                                <a href="evenements/edit.php?id=<?php echo $evenement['id_evenement']; ?>" class="btn-edit">MODIFIER</a>
                                <a href="evenements/delete.php?id=<?php echo $evenement['id_evenement']; ?>" class="btn-delete" onclick="return confirm('Êtes-vous sûr ?')">SUPPRIMER</a>
                            </td>
                        </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            <?php endif; ?>
        </div>
    </div>

    <?php include_once 'views/back/footer.php'; ?>
</body>
</html>